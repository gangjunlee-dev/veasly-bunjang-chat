{
  "name": "bunjang-monitor-cloud",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && wrangler pages deploy dist",
    "preview": "wrangler pages dev dist --kv=MONITOR_KV --port 3000 --ip 0.0.0.0"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "vite": "^5.4.0",
    "wrangler": "^3.114.0",
    "typescript": "^5.7.0"
  }
}
